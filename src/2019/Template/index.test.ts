import {} from '.';

describe('day X', () => {
  describe('part 1', () => {
    describe('', () => {
      test('', () => {
        expect(1).toBe(1);
        expect(1).toBe(1);
      });
    });
  });
  describe('part 2', () => {});
});
