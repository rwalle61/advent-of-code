import {

} from '.';

describe('day X', () => {
  describe('part 1', () => {
    describe('', () => {
      test('', () => {
        expect(1).toEqual(1);
        expect(1).toStrictEqual(1);
      });
    });
  });
  describe('part 2', () => {
  });
});
